/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Dev TLS certificates (mkcert - see setup-dev-https.ps1)
/storage/certs

docker/nginx-tls.rendered.conf

/DEPLOYMENT.md
/deployment.local.md
/AGENTS.md

# Working planning documents stay local-only
/*_PLAN.md

# Local databases: this repo is a single source for CODE; every machine
# creates and transfers its own database (Docker MySQL in production, or a
# local SQLite file for ad-hoc runs), so no local DB artifact is committed.
/public-hosted
/torogym
*.sqlite
*.sqlite3
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
/database/*.sqlite
/database/*.sqlite3

# Database transfer packages (scripts/push-server-data.ps1 -> restore-server-data.ps1)
/toro-db.sql.gz
/toro-storage.tar.gz
/scripts/toro-source.env
